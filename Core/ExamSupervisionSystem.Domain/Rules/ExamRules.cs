using System;
using System.Collections.Generic;
using System.Linq;
using System.Security;
using System.Text;
using System.Threading.Tasks;


namespace ExamSupervisionSystem.Domain.Rules
{
    //public class ExamRules
    //{
    //    public object Permission { get; private set; }

    //    public bool CanCreateExam(UserDto user, CourseDto course)
    //    {
    //        // Kullanıcının gerekli izinlere sahip olup olmadığını kontrol etme
    //        if (user.HasPermission(Permission.CreateExam) && user.HasPermission(Permission.AssignSupervisor))
    //        {
    //            // Dersin tarihini ve saatini kontrol etme
    //            if (IsExamDateTimeValid(course.ExamDate, course.ExamTime))
    //            {
    //                return true;
    //            }
    //        }
    //    }
    //}

}
